import { Component } from '@angular/core';

@Component({
  selector: 'app-agent-package',
  templateUrl: './agent-package.page.html',
  styleUrls: ['./agent-package.page.scss']
})
export class AgentPackagePage {
  description = 'Shows steps to zip agent package and import via Teams Developer Portal and Copilot.';
}
